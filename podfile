# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'scrollview_lesson' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for scrollview_lesson
    pod "PGFramework"
  target 'scrollview_lessonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'scrollview_lessonUITests' do
    # Pods for testing
  end

end
